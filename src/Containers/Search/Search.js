import React from 'react';

const Search = () => {
  return (
    <div>
      search component
      search component
      search component
      search component
      search component
      search component
      search component
      search component
      search component
      search component
      search component
      search component
      search component
      search component
      search component
      search component
      search component
      search component
      search component
      search component
      search component
      search component
      search component
      search component
      search component
      search component
      search component
    </div>
  );
}

export default Search;
